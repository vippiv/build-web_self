/**
 * +----------------------------------------------------------------------
 * | config
 * +----------------------------------------------------------------------
 * | Author: 470211273@qq.com
 * +----------------------------------------------------------------------
 */
var config = {
    // 用户自主建站主体部分（所有元素都放这里）
    buildWebBox: $("#qz_build_web_box"),

    // 自主建站设置工具箱，只要添加元素就要启用它
    eleSetBox: $('#ele-setting-box'),

    // 当前元素的样式
    currentEleStyle: {},

    // 当前活动的元素
    currentEle: null,

    // 基础块
    baseBlockHtmlStr: '<div class="tip-border"><div class="iconfont icon-edit icon-position"></div></div>'
}
